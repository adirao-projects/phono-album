
import * as React from "react";

export default function AddImage(placement) {
        const media = useState([
            {contentType: "image"},
        ]);
    
        return (
        <div>
            
            <AddImage type={"image"}/>
        </div>

    )
  }